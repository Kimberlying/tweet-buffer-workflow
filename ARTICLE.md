# 一个 Skill，串起我的多模型推文工作流

我把原来需要理解多个 Python 文件、模型配置和 Buffer 接口的流程，收敛成了一个可以直接复用的 Skill：`tweet-buffer-workflow`。

它的作用不是替别人决定发什么，而是把一份原始素材变成可审核的 X 推文，再把用户明确批准的结果交给 Buffer。使用者只需要把 Skill 安装到自己的 Codex 环境，然后在对话里调用：

```text
Use $tweet-buffer-workflow to process the material below.
```

接着粘贴文字，或者发送图片和 Caption。Skill 会先识别输入，再让使用者选择模型和处理方式。

## 怎么安装

解压交接包后，把 `tweet-buffer-workflow` 目录放进 Codex 的 Skills 目录：

```bash
mkdir -p ~/.codex/skills
cp -R tweet-buffer-workflow ~/.codex/skills/
```

如果使用者设置了自定义 Skills 根目录，就把目标目录替换为该目录。重新打开 Codex 后，就可以显式调用 `$tweet-buffer-workflow`。如果当前环境支持自动 Skill 发现，也可以直接描述“把这段内容改成 X 推文并准备 Buffer Draft”。

## 模型怎么选

现在可选五个模型：Grok 4.6、Codex 5.6 Luna、DeepSeek V4 Flash、Gemini 3.8 Flash 和 Gemini 3.7 Flash。

我保留多个模型，是因为它们在同一份素材上的表达会不同。我的实际使用判断是，Gemini 的中文语感和 X 网感更贴近我想要的方向，所以经常优先使用 Gemini。3.7 是手动备用选项：如果 3.8 遇到服务端容量问题，就重新选择 3.7，不让系统偷偷替换模型。

## 两种处理方式

第一种是“润色”。它保留原文的核心观点和事实，只把表达改得更适合 X：首行更快进入主题，长段落变成手机端容易阅读的短段落，控制在 250 个字符以内，不强行添加互动 CTA，也不以问号收尾。

第二种是“翻译”。它会先判断语言：英文转自然的简体中文，繁体中文转简体中文，已经是简体中文时只做必要润色。重点不是逐字翻译，而是保留原文语境、语气、数字、专有名词和事实边界。

无论选哪个模型，输出都先停在审核稿，不会直接发出去。

## 审核后怎么进入 Buffer

审核稿确认后，使用者只需要选择一个动作：

- `Draft`：存入 Buffer 草稿，不发布；
- `进入队列`：放入下一个可用排期；
- `队列置顶`：放到即将发布的第一条；
- `现在发送`：立即发布到 X；
- `重出`：不写入 Buffer，重新生成一个版本。

如果 Buffer 的普通队列已经达到容量上限，`进入队列` 会自动改存 Draft，并告诉使用者队列已满。`队列置顶` 不会被静默改成 Draft。这样容量问题不会导致内容丢失，也不会改变原本的发布意图。

## 图片怎么处理

图片和文字可以在同一次调用里处理。文字 Caption 走正常的润色或翻译流程，图片则单独做 OCR、繁简转换和风格处理：

1. 识别繁体中文并转换为简体中文；
2. 图片里有英文时，询问是否翻译成中文；
3. 清理第三方账号或 ID，只有明确提供时才加入自己的 ID；
4. 按白色或浅蓝色科技风生成竖版信息图，内容从上到下排列；
5. 文案和图片分别通过审核后，才允许作为图文内容交给 Buffer。

## 使用者需要准备什么

Skill 本身不携带任何 Token，也不保存别人的账号。要使用真实 Buffer 写入，需要使用者自己的 Buffer MCP 或连接器，以及目标 X 频道权限。模型 API Key、Telegram Token、Buffer Key 和 Channel ID 都放在自己的受保护配置中，不粘贴进 Skill、不提交到 Git。

如果没有 Buffer 连接，Skill 仍然可以完成改写和预览，但只会返回可复制的最终文本和发布参数，不会假装已经发出。

## 这个 Skill 的价值

它把复杂性藏在一个稳定的使用入口后面：使用者不需要记住每个模型的 API 格式，也不需要手动拼 Buffer 请求。模型可以替换，版本可以升级，但流程始终保持一致：

```text
输入素材 → 选择模型 → 润色或翻译 → 预览审核 → 选择 Buffer 动作
```

我保留的核心原则只有三个：原意和事实不能被改坏，外部发布必须有明确动作，任何不确定的发布结果都先停下来核对。
